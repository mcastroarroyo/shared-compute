# QA report — overnight run, 2026-09-06 (GCP production)

Target: `https://api.ayni-ai.com` on Cloud Run (`ayni1-507216`, us-east1), Mac provider + Pixel 9 Pro Fold (device_attested) attached. Run unattended after the Play Store submission work.

## Summary

| Area | Result |
|---|---|
| Unit tests, Go (`coordinator`) | all 15 packages pass, `go vet` clean |
| Unit tests, Rust (`provider-core`, excl. sc-mobile) | all pass |
| End-to-end encrypted round trip (`make e2e`) | PASS: stream + block + auth + log hygiene + metrics |
| Android unit tests | pass; lint enforced, release AAB v0.2.1 (3), targetSdk 36 |
| Site / web app typecheck | pass (no lint script configured) |
| Functional QA (`scripts/qa-prod.sh`, live) | **32 / 35**; the 3 misses are `insufficient_credit` on the unfunded test key |
| Security probes at the edge | see findings; **two real gaps found and fixed** |
| Load, 20 concurrent completions | 20/20 HTTP 200, no 5xx; queueing latency high (2 tiny providers) |
| Resilience | admin drain reattaches providers in seconds; provider killed mid-job → client still got a 200 answer from the other node; 5× provider churn → coordinator healthy |
| Monitoring | uptime check every 60 s, 2 alert policies enabled, automated Cloud SQL backups succeeding |
| Web surfaces | ayni-ai.com, /privacy/, /security/, /council/, app.ayni-ai.com (signed-in dashboard renders), demo.ayni-ai.com, models manifest: all 200 |

## Findings

### 1. Cloud Armor WAF never evaluated (fixed)
The per-IP throttle rule sat at priority 1000 with match `true`. Cloud Armor stops at the first matching rule, so every request was accepted by the throttle and the SQLi/XSS/LFI/RCE/protocol/scanner rules at 2000–2005 never ran. LB logs confirmed `outcome=ACCEPT priority=1000` for injection payloads.
**Fix:** throttle recreated at priority 5000 (same limits: 300 req/min/IP → 429), rule 1000 deleted. Verified: `' OR 1=1--`, `UNION SELECT`, `<script>`, `../../etc/passwd` now return **403** with `outcome=DENY priority=2000–2002`; a burst of 330 requests still yields 429s (16 of 330).

### 2. `/metrics` publicly readable (fixed)
Prometheus metrics (route names, provider counts, latency histograms, 431 lines) were served to anyone. Now behind the admin token whenever one is configured (production); regression test `TestMetricsRequireAdminInProduction`.

### 3. Supply-chain CI had never run (fixed)
`.github/workflows/supply-chain.yml` contained two unquoted `${{ }}` expressions inside YAML flow mappings, so GitHub rejected the file and every run since the workflow was added failed before any scanner executed. Quoted both; the workflow now runs and, on its first real execution, found:
- **govulncheck:** the KMS `AsymmetricSign` path reached a vulnerable http2 transport via `golang.org/x/crypto` 0.54 → upgraded to 0.55 (+ grpc 1.83.2). All Go packages still pass; coordinator rebuilt and redeployed (revision 00010).
- **pip-audit (security harness):** `cryptography` 46.0.0 had 4 advisories → 50.0.0; the 32 harness tests pass.
- **npm audit (site, webapp, console, demo):** `next` 15.1.6 (critical dev-server advisory), `postcss` 8.4.31 (high), `sharp` 0.33 (high) → next 15.5.25, sharp 0.35, postcss 8.5.28 with an npm override for the copy bundled inside next. Typecheck and production builds pass for all four; audit reports 0 vulnerabilities. Cloudflare Pages redeployed them.
- **gitleaks, cargo-audit:** clean.
The workflow is now green end to end.

### 4. Per-key rate limit
140 rapid `/v1/models` calls with one key → 130×200 then 10×429 with `Retry-After`; recovered after the window. Works as configured (120/min).

### 5. Capacity, not a bug
20 concurrent 40-token completions all succeeded but queued: TTFT p50 65 s, p95 108 s, max 108 s. Two devices, one job at a time each. Requests wait rather than fail; the quote engine's ETA already models this. More providers is the only fix.

### 6. CORS (observation)
Non-app origins receive `Access-Control-Allow-Origin: *` without credentials, which is the standard posture for a bearer-key API. Localhost origins are credentialed for local development; acceptable for now, revisit before GA.

## Verified controls
- TLS 1.3 at the edge, TLS 1.1 rejected, HTTP→HTTPS 301.
- HSTS preload, nosniff, DENY framing, no-referrer, CSP `default-src 'none'`, `cache-control: no-store` on every response.
- Auth boundaries: unauthenticated and bad-token calls to chat, workloads, admin and drain → 401; bad methods → 405.
- 2 MB body with a valid key → 413.
- WebSocket without registration frame → 426.
- Stripe: live checkout session creation works; both webhook routes reject bad signatures (400).
- GitHub OAuth: `/auth/github` → 302 to GitHub; app dashboard renders signed-in.
- Council read endpoints and the KMS manifest key served on the real hostname.
- Attestation: Pixel registers `device_attested` after the key-regeneration fix and served 6/6 pinned jobs at ~18 tok/s.
- Deploy hand-over: after each deploy the previous revision is drained; providers reattach within seconds (validated on three consecutive deploys tonight: `/metrics`, Go deps, and the earlier drain rollout).
- Play Console: internal testing track is Active with release 2 (0.2.0); the targetSdk-36 bundle 0.2.1 (3) was uploaded but not yet rolled out.

## Android app, emulator run (no phone attached)
Fresh install of the release APK on an arm64 Android 15 emulator, configured only with the Play-review registration token:
- Registers as `community` (no hardware attestation on an emulator) with signed-manifest verification on by default.
- With the Mac and Pixel paused it served 2 real demo jobs end to end (47 tokens each, ~3.5 tok/s on the emulated CPU); UI counters updated; foreground notification present.
- **Bug found:** after a device reboot the boot receiver starts the foreground service, Android 15 rejects `FGS type dataSync … from BOOT_COMPLETED`, and the service crash-loops (ActivityManager keeps restarting it). Fix below.
- **Fix verified on the emulator with 0.2.2 (4):** reboot → no crash, a "Device restarted — tap to resume sharing" notification appears; tapping it re-registers the node in 5 s. Run-policy check: unplugging the charger (with discharging status) flips the node to `blocked · waiting for charger` within seconds and removes it from the coordinator; plugging back in re-registers it in 10 s.
- Note: an earlier "mock backend" reading came from a stale August install on the emulator, not from the shipped APK, which contains no token and defaults to llama.

## Desktop provider, public installer path
Ran `curl -fsSL https://api.ayni-ai.com/install/provider.sh | SC_REGISTRATION_TOKEN=… bash` as a brand-new user in an isolated directory.
- **Blocker found:** the GitHub repository is **private**, so the installer's prebuilt-binary download (`releases/download/provider-latest/…`) returns 404 to anonymous users and the script silently falls back to a from-source Rust build. The public site also links the repo as open source under Apache 2.0. Decision needed: make the repository public, or host the binaries on the public `models.ayni-ai.com` bucket (needs an R2 write token in CI).
- With the freshly built macOS binary pre-placed, the rest of the flow works: 491 MB model pulled from the signed registry, node registered as `community`, and with the other nodes paused it served demo jobs at ~90 tok/s on an M1 Pro.
- The served installer did not pin the coordinator's manifest key, so desktop nodes ran without signed-manifest verification. Fixed in code (`/install/provider.sh` now fetches `/v1/manifest-key` and runs with `SC_REQUIRE_MANIFEST=1`); ships with the next coordinator deploy. Download failures are no longer hidden.
- `release-provider.yml` used delete-then-create for the rolling release, leaving a window where installs 404; now uploads in place. A fresh build from `main` was dispatched.

## Coordinator deploy with the pinned-key installer (after gcloud re-auth)
Deployed revision 00014 → the served `/install/provider.sh` now pins the KMS signing key; a fresh public install logged `workload manifest verification enabled keys=1 require=true` and registered. The rollout exposed a hand-over gap: the serving revision was untagged, so the drain had no URL and the Mac node stayed stranded. `deploy.sh` now tags the serving revision (`prev-NNNNN`) before every rollout, drains it afterwards and removes the temporary tag; validated on revision 00017 (`drained: 1`, provider back in 10 s).

## Play-installed app on the Pixel (real user path)
Installed from the internal-testing link (`installerPackageName=com.android.vending`, 0.2.2). First attempt failed with `registration rejected: bad-token`: the Share page's **Copy** button copies the whole install command, and that command had been pasted into the app's token field. With the bare token the phone registered as `device_attested` on the live coordinator under the owner's own account. Fixes: the Share page now has a "Phone app" card with just the token and a Copy button; the app normalises whatever is pasted (extracts `sc_prov_…` from a pasted command); Save settings shows a visible "Saved ✓" state. Shipped as 0.2.3 (5).

## Web app (app.ayni-ai.com), desktop browser
Signed in as the owner. Dashboard, Share, Run and Earnings all render with zero console errors; the session cookie is HttpOnly (not readable from JavaScript); `/v1/me*` return 401 without or with a bad session; 404 page works.
- **Run a workload:** pasted a paragraph → quote $0.01, ETA 3 s, 2 devices, Council APPROVE with three seats and an audit id. Accept with a $0 balance shows "Not enough credit for this quote" with an "Add $10 credit" button. That button opens a live Stripe Checkout for $10 "Ayni inference credits" under Zalesgen LLC (not paid).
- **Earnings:** shows the phone's jobs (3) and 2 linked devices; sub-cent totals rendered as $0.00 → now shown with five decimals.
- **Share:** new "Phone app" card with the bare token (the old page only embedded it in commands, which caused the bad-token paste on the phone).
- **Bug found and fixed:** Stripe Checkout's success and cancel URLs point at `/billing/?topup=…`, and the web app had no `/billing/` page, so paying or cancelling ended on a 404. Added the page (shows the outcome, polls the balance after success, links to Run). Verified the cancel return live.
- **Sign out:** redirects to the sign-in page and `/v1/me` returns 401 afterwards.
- **Headers:** the Pages sites sent no HSTS/CSP/X-Frame-Options → added `_headers` (HSTS preload, frame-ancestors none, nosniff, referrer, permissions-policy) to all four sites; live on ayni-ai.com, app and console; demo pending its next build.

## Still open
- QA key funding (3 remaining QA misses) — one live checkout.
- Fly.io wind-down after 24 h clean on GCP (cutover 2026-09-05 ~23:15 UTC).
- ~~Repository visibility~~ — made **public** on 2026-09-06 after a pre-publication scan (no tracked secrets, gitleaks clean, Apache-2.0 LICENSE present); anonymous asset download now returns 200.
- Lint scripts for `site` and `webapp` (none defined).
- External pen test, legal review of privacy/terms, real Council roster.

## User-journey walkthrough in Chrome (evening, visible to the user)

Path: ayni-ai.com → live quote widget → Run a workload → app.ayni-ai.com dashboard →
Run (paste doc, quote $0.01, 3 devices, Council APPROVE ×3 with audit id) → Accept with
$0 balance → "Not enough credit" + Add $10 → Stripe Checkout (not paid) → Earnings →
Share. Every step rendered without console errors.

Findings and fixes (commit 77c1311):

1. **Marketing site never linked to the app.** Hero "Run a workload" / "Share your compute"
   led to /rent/ and /share/, whose only CTA was the waitlist. A visitor could not reach
   sign-in. Fixed: "Open the app" button in the nav, and "Run a workload now" /
   "Share your computer now" primary CTAs on /rent/ and /share/ pointing at app.ayni-ai.com.
2. **Earnings showed $0.00 against 3 completed jobs.** `pricing.QuoteJob` truncated
   micro-USD: a 10-token reply on a MICRO model is 0.8 µ$ → 0. Fixed with `math.Round`
   on base and provider share (tests unchanged, still pass), plus an explanatory note on
   the Earnings page when jobs > 0 and owed == 0. Coordinator redeployed.
3. Payout onboarding is operator-run (`/admin/payouts/*`) by design; the Earnings page
   says so. No user-facing Stripe Connect button yet — acceptable for the founders-only
   internal test, revisit before open testing.
4. Share page phone card has the token but no Play link yet; add the store URL once the
   listing is public.

Play Console: screenshots (phone/7"/10") and IARC rating are complete. Three new
declarations block the 0.2.3 rollout (Advertising ID, Foreground service permissions
with video https://ayni-ai.com/play/fgs-data-sync.mp4, Financial features). Steps handed
to the user.

## Ayni Admin console (evening, 2026-09-06 → 07)

Built for the IT-admin team on top of the operator console (`console/`, Cloudflare Pages
`ayni-console`). Coordinator endpoints in `coordinator/internal/api/admin_console.go`;
event ring in `coordinator/internal/events`; dev fleet simulator `cmd/devprovider`.

Pages: Overview (devices online, users online/total, phones / PCs / other with share of
fleet, online ACU, active jobs, 24h requests+tokens, errors 1h, fleet-by-type bars and
compute table, platform/tier/class/model chips, coordinator facts, 7d ledger, last error,
usage by key), Devices (live table joined with benchmark, owner, telemetry, battery, ledger;
type filter + search; row detail; per-device Disconnect; offline catalog), Users (accounts
with key, credit, devices online/total), Events & logs (ring of Info+ log records plus
AUDIT entries; level filter, search, live tail), Controls (pause/resume intake with
message → 503 + Retry-After on inference, drain, credit adjust ±$1000, sanitized runtime
config, Prometheus metrics), plus the existing API keys, Payouts, Playground, Settings.

### Round 1 — local coordinator (memory store) + simulated fleet, Chrome, user-visible
Fleet: 2 fake Android phones, 1 fake Windows PC, 1 fake tvOS device, the real
provider-daemon (mock backend) as a Mac. Go: `go test ./internal/api ./internal/events`
(8 new console tests) pass.

| Check | Result |
|---|---|
| Settings save + Test connection | saved; "coordinator OK — 5 provider(s)" |
| Overview counts | 5 online = 2 phones / 2 PCs / 1 other; 13.80 ACU; chips android 2, macos 1, tvos 1, windows 1 |
| Compute by kind | phone 2.38 ACU / 33.1 tok/s / 16 GB; pc 10.86 / 193.9 / 32 GB; other 0.56 / 5.5 / 3 GB — matches API |
| Devices table + detail | battery only on phones (80% ⚡); detail shows identity, CPU, models, telemetry, security, benchmark drift, owed |
| Type filter | Phone → 2 rows |
| Per-device Disconnect | count 5→4, AUDIT with reason+actor, device logged the reason and reconnected in 3 s |
| Offline catalog | stopped Windows sim appears with last benchmark (M5 · 3.22, 55.2 tok/s) |
| Pause intake | chat → 503, Retry-After 60, code `maintenance`, operator message in body; resume → accepted again |
| Drain | AUDIT "drain: 5"; all reconnected |
| Credit adjust | +$5 → balance $5.00; AUDIT with note |
| Config | no secrets; booleans only |
| Metrics | sc_* series render |
| API keys | create shows key once, lists it |
| Playground | job routed to a fake phone which declined → **gap**: no WARN in Events, empty bubble |

Fixes from round 1 (commit 838cdb3): relay logs `job failed on provider` (WARN, job/provider/
platform/code), `mapRelayErr` logs `job failed` (WARN) so the Errors tile and Events show it;
streaming error frames now carry the code and the playground renders "⚠ code: message".

Notes: Next dev server shows pages pre-hydration for a few seconds on first compile (not in
the static export). The browser automation cannot type into password fields; the Settings
form was verified through React's own change handler + the real Save button.

### Round 2 — fresh local coordinator, static console build (what Cloudflare serves)
Scripted API pass: 18/19 checks pass (the miss was the test's own threshold — 4 AUDIT rows
is correct on a freshly restarted instance). Browser pass on the static export: every page
hydrates in <4 s; Overview KPIs, Devices (5 rows, 2/2/1), Users note, Events AUDIT filter
(4 rows, exactly the round-2 actions), Controls pause → live chat 503 "round-2 UI pause"
→ resume, Playground: declined job now renders "⚠ internal: the request could not be
completed", Events shows `job failed on provider` (platform, code) + `job failed`, Overview
warnings tile = 2.

Production: coordinator 6f99cf9 on revision ayni-coordinator-00022-wav (100 %); /admin/*
answers 401 without a token; console live at https://ayni-console.pages.dev as "Ayni Admin".
Dead revision ayni-coordinator-00019-lab (image never built) makes deploy.sh report
"failed" after a successful rollout; delete it:
`gcloud run revisions delete ayni-coordinator-00019-lab --region=us-east1 --project=ayni1-507216 --quiet`.

### Round 3 — production (https://ayni-console.pages.dev, user's own admin token, in Chrome)
Overview: 3 devices online (2 phones, 1 Mac), 2 users online / 2 accounts, 8 known in
catalog, 10.31 ACU, 64 requests / 3,207 tokens in 24 h, 0 errors. Devices: both phones
`device_attested`, attributed to jepeto580@gmail.com and mcastroarroyo@gmail.com; 5
offline identities in the catalog. Users: both accounts with keys, credit and 1/1 and 1/2
devices online. Events: attestation trail visible (`android key attestation verified`
security_level=strongbox verified_boot=verified device_locked=true). Controls: config
renders; credit +$5 to the playground key audited.

**Two-phone job:** `POST /v1/batch`, 10 one-sentence prompts, concurrency 3, from the
console page with the playground key. Result: status 200, ok 10 / failed 0, fanout 3,
wall 5.4 s; per provider: phone 53c6960f (device_attested) 2 items at ~2.7 s, phone
e506b790 (device_attested) 2 items at ~1.8 s, Mac fa06d7c5 (community) 6 items at ~0.75 s.
Both phones served real llama.cpp output.

Gap found: `X-Provider-Trust-Level` was not in the CORS allow-list, so a browser client
cannot request attested-only capacity (curl can). Fixed in server.go.
Known limitation: real phones report battery/charging as null (sc_telemetry hardcodes it),
so the Battery column shows "—" for them; the dev simulator fills it.

### Post-deploy finding: stranded providers and phones that never reconnect
After the CORS deploy the fleet read 0. Cause 1: `gcloud run deploy` exits non-zero because
of the dead revision 00019, and deploy.sh (`set -e`) aborted before its drain step, so the
Mac and one phone stayed on the superseded instance (`/admin/drain` on r6f99cf9 → `drained: 2`).
Fix: deploy.sh now trusts the traffic split instead of gcloud's exit code and always drains.
Cause 2: the phone core (`sc-mobile`) ran `provider_lib::run` once; a coordinator-initiated
close put the app in "stopped" until the user tapped Start. The desktop daemon already
reconnected with backoff (re-registered 0.6 s after the drain). Fix: the same capped-backoff
reconnect loop in `sc-mobile::start` (2 s → 60 s, resets after a 20 s session), shipped as
Android 0.2.4 (versionCode 6).

**Reconnect fix verified** (`provider-core/sc-mobile/tests/reconnect.rs`, live test against
the local coordinator): first registration → `/admin/providers/{id}/disconnect` →
"coordinator closed the connection" → "reconnecting in 2s" → re-registered 2.1 s later with a
new provider id; `stop()` ends the loop. Android 0.2.4 (versionCode 6) carries it:
`~/Desktop/ayni-play-assets/ayni-provider-0.2.4-vc6.aab` (+ .apk), signed with the upload key.
Roll out 0.2.4 instead of 0.2.3; the three pending declarations (Advertising ID, Foreground
service, Financial features) still gate the rollout.

## Onboarding v2 (2026-09-07) — pairing codes, device wizard, tester loop

Shipped in 10162f1 / 73e7bb9 and deployed (Cloud Run rev for 10162f1; Pages auto-deploys):

- **Share page → "Add a device" wizard**: pick Android / Mac / Linux / Windows; phone path shows a
  6-letter pairing code with a live countdown and a "New code" button; Mac/Linux path is the one-liner
  with a Copy button and a "keep it running" hint; Windows path lists the manual steps. Section 3 polls
  `/v1/me/devices` every 4 s and shows a green "✓ Connected" banner when a new device comes online.
  Verified live: code minted, countdown ran, list showed Phone (device_attested, 18.3 tok/s, 7 jobs),
  Mac (158 tok/s, 11 jobs) and one offline identity.
- **Pairing API**: `POST /v1/pair/{code}` returned 200 with the account token (72 chars), account hint
  and coordinator URL; the same code a second time → 404 `bad_code`. Unknown code → 404 `bad_code`.
  Per-IP intake rate limit applies. Audit event "device paired" recorded.
- **Feedback loop**: `POST /v1/feedback` (200 `{"ok":true}`) → console Feedback page lists it with kind,
  device, app/version, age; filters, search and CSV export work. Web app `/testers/` page live.
- **Installer**: script parses (`bash -n`); noise filter drops ggml/llama lines and keeps
  `registered`, `benchmark: done`, `WARN … disconnected` (sample test). Banner points to the Share page.
- **Android 0.2.5 (vc 7)** built and signed (AAB on Desktop): first-run screen is now
  Status → "Pair this phone" card → Start/Stop → Settings → "Send feedback about this app".
  Ledger and gauge appear only once configured.

Emulator note: the 1080×2400 `sc_test` AVD under host memory pressure produced two ANRs
("Input dispatching timed out … FocusEvent", system load 22) before the app ran any pairing code;
the ANR trace is the system server, not the app. Created a lighter `sc_small` AVD (720×1280, 1.5 GB)
for UI tests.
