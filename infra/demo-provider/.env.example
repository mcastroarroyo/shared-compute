# A provider registration token. Either a per-account token from the "Share your
# computer" page (app.ayni-ai.com), or one of the values in the coordinator's
# SC_PROVIDER_TOKENS secret.
SC_REGISTRATION_TOKEN=

# Override only for a staging coordinator.
# SC_COORDINATOR_URL=wss://api.ayni-ai.com/ws/provider
# SC_API=https://api.ayni-ai.com
